services:
  # Backend Express Server + BullMQ Worker
  - type: web
    name: reachinbox-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build --workspace=apps/backend
    startCommand: npm start --workspace=apps/backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: SESSION_SECRET
        value: reachinbox-dev-session-secret-key-min-32-chars
      - key: JWT_SECRET
        value: reachinbox-dev-jwt-secret-key-min-32-chars

  # Frontend Next.js Web App
  - type: web
    name: reachinbox-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build --workspace=apps/frontend
    startCommand: npm start --workspace=apps/frontend
    envVars:
      - key: NODE_ENV
        value: production
